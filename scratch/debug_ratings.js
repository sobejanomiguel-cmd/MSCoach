
async function debugTournamentRatings() {
    // This is meant to be run in the browser console or via a tool if I had access to the live DB.
    // Since I don't, I will look at how I'm filtering torneos in app.js.
}
